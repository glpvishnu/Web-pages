# Web-pages
These are the web pages that I had done
install live server extension in your VS code 
Right click on the index.HTML file
Open with live server
Option 2 
Go to live location in system 
Double click the HTML file
